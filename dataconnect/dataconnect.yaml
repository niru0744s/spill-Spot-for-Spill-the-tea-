specVersion: "v1"
serviceId: "spill-811b2-2-service"
location: "us-east4"
schemas:
  - source: "./schema"
    datasource:
      postgresql:
        database: "spill-811b2-2-database"
        cloudSql:
          instanceId: "spill-811b2-2-instance"
          # schemaValidation: "STRICT"     # STRICT mode makes Postgres schema match Data Connect exactly.
          # schemaValidation: "COMPATIBLE" # COMPATIBLE mode makes Postgres schema compatible with Data Connect.
connectorDirs: ["./default_connector"]
